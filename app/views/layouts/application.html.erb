<!DOCTYPE html>
<html>
  <head>
    <title>controlwise.io</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex">
      <div class="bg-emerald-400 p-4 w-1/6 hidden md:flex flex-col justify-between sticky top-20 h-[calc(100vh)]">
        <%= render "sidebar"%>
      </div>
      <div class="w-5/6">

          <%= render "navbar"%>

          <main class="container p-4">
            <%= yield %>
          </main>
      </div>
    </div>
  </body>
</html>
